<p id="notice"><%= notice %></p>

<h1>가위바위보 결과물을 공유해 봅시다</h1>
<br>

<table class="table table-striped">
  <thead>
    <tr>
      <th>이름</th> 
      <th>제목</th>
      <th>내용</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.name %></td>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
        <td><%= link_to '보기', post, :class => "btn  btn-primary" %></td> 
        <td><%= link_to '수정하기', edit_post_path(post), :class => "btn  btn-warning" %></td>
        <td><%= link_to '삭제하기', post, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '새로운 포스트 만들기', new_post_path, :class => "btn  btn-info" %>
